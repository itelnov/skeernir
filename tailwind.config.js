/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // Enable class-based dark mode
    content: ["./templates/**/*.{html,js}"],
    theme: {
      extend: {},
    },
    plugins: [
    ],
  }